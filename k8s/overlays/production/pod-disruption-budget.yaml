apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: omniforge-backend-pdb
  namespace: omniforge-production
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: omniforge-backend
